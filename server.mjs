import http from 'node:http';import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.join(path.dirname(fileURLToPath(import.meta.url)),'dist');const port=Number(process.env.PORT||4175);const mime={'.css':'text/css','.js':'text/javascript','.png':'image/png','.svg':'image/svg+xml','.html':'text/html','.json':'application/json'};
http.createServer(async(req,res)=>{let p;try{p=decodeURIComponent(new URL(req.url,'http://localhost').pathname)}catch{res.writeHead(400);res.end();return}if(p==='/test'||p==='/test/'){res.writeHead(302,{location:'/'});res.end();return}const file=path.resolve(root,'.'+p);if(file!==root&&!file.startsWith(root+path.sep)){res.writeHead(403);res.end();return}try{const stat=await fs.stat(file);const target=stat.isDirectory()?path.join(file,'index.html'):file;const data=await fs.readFile(target);res.writeHead(200,{'content-type':(mime[path.extname(target)]||'application/octet-stream')+'; charset=utf-8','cache-control':'no-store'});res.end(data)}catch{res.writeHead(404,{'content-type':'text/html; charset=utf-8'});res.end(await fs.readFile(path.join(root,'404.html')))}}).listen(port,()=>console.log('Local: http://localhost:'+port));
